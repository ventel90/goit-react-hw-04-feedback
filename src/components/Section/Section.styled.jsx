import styled from "@emotion/styled";

export const Container = styled.section`
  max-width: 100%;
  padding: 1em;
  text-align: center;
  border-radius: 5px;
`;

export const Title = styled.h2`
  margin-bottom: 1em;
  font-size: 2em;
  color: var(--primary);
`;